server:
  port: 9012
spring:
  application:
    name: aepc-api-spring-secur-oauth2-kafka-bs-ms-address
  datasource:
    url: jdbc:mysql://localhost:3306/addresses-db?createDatabaseIfNotExist=true
 # to indicate where services registration is running on
eureka:
  client:
    service-url:
     defaultZone: http://localhost:9009/eureka/ # this is docker image of regsitraion service
         
